import React from "react";
import {Box, Typography} from "@mui/material";

export const PageLanding = () => {
    return (
        <Box margin={3}>
            <Typography variant="h2" component="h1">Ollisten</Typography>
            <Typography variant="h6" component="h2">Local AI Meeting helper</Typography>
        </Box>
    );
};
